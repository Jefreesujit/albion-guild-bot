'use strict';

const API = require('albion-api');

const getPlayerInfo = (playerName) => {
  // search for that player
  // fetch player info
  return;
}

const getPlayerInfo = (playerName) => {
  // search for that player
  // fetch player info
  return;
}
